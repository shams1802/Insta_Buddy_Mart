/// Reserved route identifiers for future deep linking and integration tests.
/// The app currently uses [MaterialApp.home] with an [AuthGate] root (no route table).
abstract final class AppRoutes {
  static const root = '/';
  static const login = '/login';
  static const main = '/main';
}
